/**
 * Form validation and UI constants.
 */

/**
 * Regular expression enforcing a minimum of 8 characters for passwords.
 */
export const PASSWORD_PATTERN = /^.{8,}$/;

/**
 * Unified spinner diameter in pixels.
 */
export const SPINNER_DIAMETER = 40;
